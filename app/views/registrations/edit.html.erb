<div class="container registration-container">
  <div class="well">
    <h1>Edit <small>a registration</small></h1>
    <p> Fields you don't fill out, will stay the same as before.
    <%= form_for(@registration, url: "/registrations/#{@registration.hashedEmail}", method: :put, html: {class: 'form-horizontal'}) do |f| %>
      <%= render 'shared/form', :f => f %>
    <% end %> 
  <div>
</div>